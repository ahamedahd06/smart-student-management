This **Flask + MongoDB** backend was an earlier full-stack iteration. Your **Interim.docx** specifies **Streamlit + SQLite** for the final coursework alignment.

Use **`../streamlit_ssms/`** as the primary runnable application. Keep this folder if you still want REST API experiments or to reuse `emotion_service.py` logic and Colab-exported models via environment variables.
